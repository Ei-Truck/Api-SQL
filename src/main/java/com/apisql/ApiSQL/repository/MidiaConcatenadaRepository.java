package com.apisql.ApiSQL.repository;

import com.apisql.ApiSQL.model.MidiaConcatenada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MidiaConcatenadaRepository extends JpaRepository<MidiaConcatenada, Integer> {
}